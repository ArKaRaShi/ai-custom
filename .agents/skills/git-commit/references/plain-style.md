# Plain Commit Style

Use this reference only when the user requests plain style or repository history establishes it.

Format the subject without an emoji:

```text
<type>(<scope>): <imperative summary>
```

Do not add decorative symbols to the subject.
